​<div class='mr10 float-right' >
    <%= link_to bookmark_path(current_user.bookmarks.find_by(board_id: board.id), board), 
    id: "js-bookmark-button-for-board-#{board.id}",
    class:"float-right",
    method: :delete,
    remote: true do %>
        <%= icon 'fas', 'star' %>
    <% end %>
</div>